# Java-A-Star-River-Crossing-Solver-
Java implementation of the River Crossing Puzzle using the A* Search algorithm. It finds the optimal (minimum time) solution for all family members to cross the river. Constraints: max two people per crossing (with lamp); crossing time is the max time of the pair. Uses a consistent heuristic.
